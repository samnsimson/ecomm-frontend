query Cart($input: [ProductInfo!]!) {
    cart(input: $input) {
        total
        products {
            id
            title
            slug
            salePrice
            retailPrice
            quantity
            total
        }
    }
}
