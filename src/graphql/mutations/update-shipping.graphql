mutation UpdateShipping($input: UpdateShippingInput!) {
    updateShipping(updateShippingInput: $input) {
        id
        title
        description
        enabled
        type
        amount
        percentage
    }
}
