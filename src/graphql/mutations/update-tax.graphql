mutation UpdateTax($input: UpdateTaxInput!) {
    updateTax(updateTaxInput: $input) {
        id
        title
        description
        type
        amount
        percentage
        enabled
        createdAt
        updatedAt
    }
}
