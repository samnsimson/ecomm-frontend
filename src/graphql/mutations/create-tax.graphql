mutation CreateTax($input: CreateTaxInput!) {
    createTax(createTaxInput: $input) {
        id
        title
        description
        type
        amount
        percentage
        enabled
        createdAt
        updatedAt
    }
}
