import { ProfileComponent } from '@/components/profile';

const ProfilePage = async () => {
    return (
        <div>
            <ProfileComponent />
        </div>
    );
};
export default ProfilePage;
